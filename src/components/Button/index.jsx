import styled from './button.module.scss';

const Button = (props) => {
  return(
    <>
      <button className={styled.button} {...props}>
        
      </button>
    </>
  )
}

export default Button;